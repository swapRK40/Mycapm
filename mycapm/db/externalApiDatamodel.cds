//These is for external API calling to our Project 

//We have use Axios NPM package for these

namespace india.db;

entity ExternalData{
    userId : Integer;
    key id : Integer;
    title : String(255);
    body : String(2000)
}
